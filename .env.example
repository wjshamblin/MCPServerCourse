# Step 13 — Financial Projections Dashboard (Capstone)
#
# Runs without auth out of the box. To gate the dashboard, copy an auth
# block from steps 08 (Duke OIDC), 09/10 (Azure OAuth), or 11 (Azure + OBO)
# into server.py and pass `auth=auth` to FastMCP(...).

# No env vars are required for the default unauthenticated demo.
